# 雪茄/斗草比价监控脚本 - 交接文档

> 本文档由 GLM-5.2 整理，供后续接力者使用。内容来自与用户的实际对话，所有接口信息均已实测验证。

---

## 1. 任务背景

用户要对 `https://cigar.cab`（茄快讯）网站的商品做比价监控与补货通知。

- 用户已有一个监控脚本，需要**调整优化**（脚本本身未提供，需向用户索取）。
- 数据源是该网站公开 API，无需鉴权。
- 目标：抓取指定商品在各渠道的价格/库存，生成通知文案，并支持持续监控。

---

## 2. 数据源（已实测）

### 2.1 品牌列表页（SSR，可直爬）

- URL: `https://cigar.cab/brands?category=cuban`
- 类型: Next.js SSR，HTML 内直接含品牌列表
- 可解析字段: 品牌中英文名、Logo URL、产品数、字母分组
- 用途: 拿到全部品牌，非本任务重点

### 2.2 商品比价接口（核心）

```
GET https://cigar.cab/api/products/{productId}/channels
```

- 无需鉴权，无签名，curl 直接返回 JSON 数组
- 无分页参数（一次返回全部渠道记录）
- 建议串行 + 间隔请求（~0.5s/次），避免限流

**返回字段说明（实测样本）：**

| 字段 | 类型 | 含义 | 示例 |
|---|---|---|---|
| `id` | int | 渠道记录 ID | 941493 |
| `brand` | string\|null | 品牌（各站叫法不一，可能为 null） | "Fribourg & Treyer" |
| `productName` | string | 商品名（各站叫法不一） | "Fribourg & Treyer: SPECIAL BROWN FLAKE 50g" |
| `specification` | string\|null | 规格（多为 null） | null |
| `spec` | string\|null | 规格数值 | "50" |
| `unit` | string\|null | 单位 | "克" |
| `price` | number | 价格 | 15.99 |
| `currency` | string | 币种 | "USD" / "HKD" |
| `isInStock` | int | 库存（1=有货，0=缺货） | 1 |
| `isFrontendVisible` | null | 前端可见性（实测均为 null） | null |
| `isNotified` | int | 是否已通知 | 1 |
| `lastNotifiedTime` | string\|null | 最近补货通知时间 | "2026-06-23 04:27:36" |
| `brandCn` | null | 品牌中文名（实测为 null） | null |
| `productNameCn` | null | 商品中文名（实测为 null） | null |
| `recommendScore` | int\|null | 推荐分（1-4） | 3 |
| `isNotPush` | int | 是否不推送 | 0 |
| `productUrl` | string | 源站购买链接 | "https://www.4noggins.com/..." |
| `sourceSite` | string | 源站域名 | "4noggins.com" |
| `tag` | string\|null | 站点分类 | "欧洲斗草站" / "亚洲斗草站" |

**实测样本（productId=8721，F&T 特别布朗切片）：**

```json
[
  {
    "id": 941493,
    "brand": null,
    "productName": "Fribourg & Treyer: SPECIAL BROWN FLAKE 50g",
    "specification": null,
    "spec": "50",
    "unit": "克",
    "price": 15.99,
    "currency": "USD",
    "isInStock": 1,
    "productUrl": "https://www.4noggins.com/collections/tinned-tobacco/products/fribourg-treyer-special-brown-flake-50g",
    "sourceSite": "4noggins.com",
    "tag": "欧洲斗草站",
    "recommendScore": 3,
    "lastNotifiedTime": "2026-06-23 04:27:36"
  },
  {
    "id": 64576,
    "brand": "KOPP",
    "productName": "Fribourg & Treyer - Special Brown Flake 50 gram",
    "spec": "50",
    "unit": "克",
    "price": 152,
    "currency": "HKD",
    "isInStock": 1,
    "productUrl": "https://tobaccolifestyle.com/products/...",
    "sourceSite": "tobaccolifestyle.com",
    "tag": null,
    "recommendScore": 4
  }
]
```

---

## 3. 监控目标商品清单（用户提供）

共 15 个斗草商品 ID：

| ID | 用户标注名称 |
|---|---|
| 7180 | 黄绞盘 |
| 8718 | F&T 小白 |
| 8721 | F&T 特别布朗 |
| 8717 | F&T 混合切 |
| 8929 | GH 光明 |
| 11627 | PH 金切 |
| 11693 | PH 伪姑 |
| 12509 | SG FV |
| 12513 | SG FVP |
| 12550 | SG 山姆 |
| 12570 | SG 圣詹 |
| 12567 | SG 圣詹砖 |
| 12463 | SG 最佳布朗 |
| 12483 | SG 马车夫 |
| 14633 | 威棍子 |
| 14226 | 威塞克斯金砖 |

> 注：ID 为 4 位数到 5 位数，均为斗草类商品（非古巴雪茄）。

---

## 4. 功能要求

### 4.1 核心抓取
- 输入: 上述 15 个 productId
- 调用: `/api/products/{id}/channels`
- 串行 + 间隔（默认 0.5s），失败 ID 记录到错误日志

### 4.2 通知文案要求（用户明确强调）
> "通知文案要充分利用接口信息，名称 规格 价格 站名 地址等"

每个产品的通知文案应包含：
- 商品名称（`productName`，可能多个站叫法不同）
- 规格（`spec` + `unit`）
- 各渠道价格（`price` + `currency`）
- 站点名（`sourceSite`）
- 购买地址（`productUrl`）
- 库存状态（`isInStock`）
- 补货时间（`lastNotifiedTime`，如有）

### 4.3 输出形式（用户未明确选择，建议默认）
- 终端打印通知文案（每产品一条多行消息）
- 同时落盘 raw JSON 备用
- CSV 可选

### 4.4 过滤/排序（用户未明确，建议默认）
- 全部渠道都展示，缺货标注状态
- 按币种分组，价格升序，最低价标星

---

## 5. 待讨论事项

### 5.1 持续监控方案（用户提出待讨论）
用户原话："咱们先把功能实现，然后讨论下持续监控方案"

可能方向（待与用户确认）：
- 定时任务（cron / launchd）周期性抓取
- 状态变化检测（与上次对比，只在库存/价格变化时通知）
- 通知渠道（终端 / 邮件 / Webhook / Server酱 / Bark 等）
- 监控频率（小时级 / 天级）

### 5.2 现有脚本
用户提到"我有一个监控脚本，但需要调整优化"——**脚本内容未提供**，接力者需先向用户索取现有脚本，再在其基础上优化，避免重写。

---

## 6. 实测命令参考

```bash
# 单个商品比价
curl -sL "https://cigar.cab/api/products/8721/channels" \
  -A "Mozilla/5.0" \
  -H "Accept: application/json" \
  -H "Referer: https://cigar.cab/"

# 品牌列表（SSR HTML）
curl -sL "https://cigar.cab/brands?category=cuban" -A "Mozilla/5.0"
```

---

## 7. 注意事项

1. **接口无鉴权**，但请保持礼貌请求频率（串行 + 间隔），避免给对方服务器造成压力或被限流。
2. `brand` / `brandCn` / `productNameCn` 等字段在渠道记录里经常为 null，通知文案应以 `productName` 为主，**不要依赖中文名**。
3. 同一商品在不同站点的 `spec`/`unit` 可能不一致（有的有值有的 null），文案展示时要做容错。
4. `price` 币种混杂（USD/HKD 等），排序或对比时需按币种分组，不要直接跨币种比大小。
5. 网站备案信息：海南嘉实云启科技有限公司 琼ICP备2025058274号-1，数据源为商业站点，使用请遵守对方 ToS。

---

## 8. 接力者下一步建议

1. 先向用户索取**现有监控脚本**，理解现状。
2. 在现有脚本基础上，按本文档第 4 节功能要求调整通知文案与抓取逻辑。
3. 跑通单个 ID（如 8721）验证，再批量跑 15 个。
4. 与用户讨论第 5.1 节持续监控方案。
